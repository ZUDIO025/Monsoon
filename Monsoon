<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>ZUDIO Monsoon Offer</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background: linear-gradient(135deg, #e0f7fa, #fff3e0);
      font-family: 'Segoe UI', sans-serif;
      text-align: center;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      overflow: hidden;
    }

    .container {
      background: white;
      border-radius: 16px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
      padding: 40px 30px;
      max-width: 400px;
      animation: slideDown 1s ease-out;
    }

    @keyframes slideDown {
      from {
        transform: translateY(-50px);
        opacity: 0;
      }
      to {
        transform: translateY(0);
        opacity: 1;
      }
    }

    h1 {
      color: #00796b;
      margin-bottom: 0;
      font-size: 24px;
    }

    h2 {
      color: #00796b;
      margin-top: 10px;
      font-size: 18px;
      font-weight: 400;
    }

    button {
      margin-top: 30px;
      padding: 14px 28px;
      font-size: 16px;
      font-weight: bold;
      color: white;
      background: #ff6f00;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: background 0.3s ease;
    }

    button:hover {
      background: #ff8f00;
    }

    .coupon {
      margin-top: 25px;
      font-size: 28px;
      font-weight: bold;
      color: #d32f2f;
      background: #fff8e1;
      border: 2px dashed #d32f2f;
      padding: 16px;
      border-radius: 10px;
      display: none;
      animation: popIn 0.5s ease forwards;
    }

    @keyframes popIn {
      0% {
        transform: scale(0.6);
        opacity: 0;
      }
      100% {
        transform: scale(1);
        opacity: 1;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>You are selected for the Monsoon Offer,</h1>
    <h2>By Zudio</h2>

    <button onclick="showCoupon()">🎁 Generate Coupon Code</button>

    <div class="coupon" id="couponCode">KAZHAPPU025</div>
  </div>

  <script>
    function showCoupon() {
      const coupon = document.getElementById("couponCode");
      coupon.style.display = "block";
      coupon.style.animation = "popIn 0.5s ease";
    }
  </script>
</body>
</html>
